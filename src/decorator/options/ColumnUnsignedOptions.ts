/**
 * Options for columns that can define unsigned attribute.
 */
export interface ColumnUnsignedOptions {
    /**
     * Puts UNSIGNED attribute on to numeric column. Works only for MySQL.
     */
    unsigned?: boolean
}
